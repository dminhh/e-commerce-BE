server:
  port: 7081
spring:
  application:
    name: product
  servlet:
    multipart:
      max-file-size: 5MB
      max-request-size: 5MB
#  sql:
#    init:
#      mode: always
#      data-locations: classpath:product4.sql
#      encoding: UTF-8
  elasticsearch:
    uris: http://localhost:9200
    username: elastic
    password: changeme
#  data:
#    elasticsearch:
#      client:
#        rest:
#          uris: "http://localhost:9200"  # Địa chỉ Elasticsearch
#          username: "elastic"         # Tên người dùng Elasticsearch
#          password: "changeme"      # Mật khẩu Elasticsearch
#      repositories:
#        enabled: true
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: update

    database-platform:
      org.hibernate.dialect.MySQLDialect
    show-sql:
      false

  datasource:
    driver-class-name:
      com.mysql.cj.jdbc.Driver
  batch:
    job:
      enabled:
        false
    jdbc:
      initialize-schema:
        ALWAYS
  main:
    allow-bean-definition-overriding:
      true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}

# Cloudinary Configuration
cloudinary:
  cloud-name: ${CLOUDINARY_CLOUD_NAME:dggwp5cy2}
  api-key: ${CLOUDINARY_API_KEY:338747247774813}
  api-secret: ${CLOUDINARY_API_SECRET:hyA6yc5ZGaGoG_kltKMU5LIsVHw}

# Chatbot Configuration
chatbot:
  flask:
    url: http://localhost:5001
    chat-endpoint: /chat
    timeout-ms: 30000  # 30s cho streaming

# Logging
logging:
  level:
    com.prod.RAG: DEBUG
    reactor.netty: DEBUG