spring:
  application:
    name: information
#  sql:
#    init:
#      mode: always
#      data-locations:
##        - classpath:city.sql
#        - classpath:district.sql
#        - classpath:wards.sql
#      encoding: UTF-8
  jpa:
    hibernate:
      ddl-auto: update
server:
  port: 6066

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
    instance-id: ${spring.application.name}:${spring.application.instance_id:${random.value}}